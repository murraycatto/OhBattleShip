%h1 Listing Previous Games

%table.table.table-hover
  %thead
    %tr
      %th Name
      %th Email
      %th
      %th

  %tbody
    - @users.each do |user|
      %tr
        %td= user.name
        %td= user.email
        %td= link_to 'Resume Game', game_path(user), class: "btn btn-info"
%br
= link_to 'New Game', new_user_path, class: "btn btn-success"
